#include <stdio.h>
#include<math.h>
int main() {
    // int a;
    // printf("entr the number");
    // scanf("%d",&a);
    // int root = sqrt(a);
    // printf("the suare root is: %d",root);
    int a,b;
    printf("enter the number of a and b  : ");
    scanf("%d %d",&a,&b);
   int r=pow(a,b);
   printf(" a ki power b is %d :", r);
    return 0;
}